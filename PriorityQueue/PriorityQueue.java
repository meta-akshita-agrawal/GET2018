
public interface PriorityQueue {

	void add(Item item);//Adds an item in the priority queue
	void remove();//removes an item from priority queue
	Item peek();//displays the front item in the queue
}
